import { FormatSimplexResultPipe } from './format-simplex-result.pipe';

describe('FormatSimplexResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSimplexResultPipe();
    expect(pipe).toBeTruthy();
  });
});
